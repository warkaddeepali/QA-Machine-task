# FieldForceConnect Automation

Maven + Selenium WebDriver + TestNG + Page Object Model.

## Structure

- actions: business-level reusable actions
- elements: locators only
- pages: page-level interactions
- managers: driver, waits and configuration
- tests: TestNG test classes

## Before running

1. Replace username and password.
2. Run:

mvn clean test
